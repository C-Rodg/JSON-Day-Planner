// Import Action types

// Set initial settings state

const INITIAL_STATE = {
    schedule : {}
};

export const settings = ( state = INITIAL_STATE, action) => {
    switch(action.type) {

        default: 
            return state;
    }
}