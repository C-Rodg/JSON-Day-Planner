// Import Action types

// Set initial settings state

const INITIAL_STATE = {
    schedule : {},
    militaryTime: true
};

export const settings = ( state = INITIAL_STATE, action) => {
    switch(action.type) {

        default: 
            return state;
    }
}